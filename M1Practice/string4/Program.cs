using System.Collections;
using System.Text;
public class Program
{
    public static void Main(string[] args)
    {
        string? s="apple";


        // string trimmed=s.Trim();
        // Console.WriteLine(trimmed);
        // if (trimmed.StartsWith("CSharp"))
        // {
        //     Console.WriteLine("true");
        // }
        // if (trimmed.EndsWith("Programming"))
        // {
        //     Console.WriteLine("true");

        // }
        // Console.WriteLine("true");

        // Console.WriteLine(s.IndexOf('a'));
        // Console.WriteLine(s.LastIndexOf('a'));
        // Console.WriteLine(s.Contains("nan"));

        // Console.WriteLine(s.Substring(11,6));
        // string rep=s.Replace("csharp","dotnet");
        // Console.WriteLine(rep.ToUpper());

        // Console.WriteLine(s.ToLower()=="admIn".ToLower()?1:0);


        // string temp=s.TrimStart();
        // string rep=temp.Replace(",",";");
        // Console.WriteLine(rep);

        // string temp=s.Substring(0,4);
        // temp+="2026";
        // Console.WriteLine(temp);

        // var arr=s.Split([' ',','],StringSplitOptions.RemoveEmptyEntries);
        // Console.WriteLine(arr.Length);
        // for(int i = 0; i < arr.Length; i++)
        // {
        //    // if(string.IsNullOrWhiteSpace(arr[i]))continue;
        //     arr[i]=new string(arr[i].Reverse().ToArray());
        //     Console.Write(arr[i]+" ");
        // }
        
        // string t="yash";
        // Console.Write(t.Any(x=>char.IsLetter(x)));
        // var temp=s.Split(" ").Distinct().ToList();
        
        // string t="yash";
        // // string re=t.Remove(t.Length-3)/*  */;
        // string re=t.Insert(2,"xyz");
        // Console.Write(re.ToUpper());

        // int c=s.Count(x=>x=='a');
        // Console.WriteLine(c);
        // bool isletter=s.All(x=>char.IsLetter(x));
        // Console.WriteLine(isletter);

        // StringBuilder sb=new();
        // for(int i = 0; i < s.Length; i++)
        // {
        //     sb.Append(s[i]);
        //     if(char.IsDigit(s[i]))sb[i]='*';
        // }
        // Console.Write(sb.ToString());
        // int c=s.Count(x=>char.IsDigit(x));
        // Console.Write(c);
        // var temp=new string(s.OrderBy(x=>x).ToArray());
        // Console.WriteLine(temp);
        
        // int n=int.Parse(Console.ReadLine());
        // string a=Console.ReadLine();
        // var arr=a.Split(" ").ToList();
        // List<int>ar=new();
        // foreach(var i in arr)
        // {
        //     int t=Convert.ToInt32(i);
        //     ar.Add(t);
        // }
        // var odd=ar.Where(x=>x%2==0);
        // foreach(var i in odd)
        // {
        //     Console.Write(i);
        // }
    
    }
}


