@using Microsoft.AspNetCore.Identity
@model List<IdentityUser>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>


<h2>All Users</h2>

@foreach (var user in Model)
{
    <div>
        @user.Email
        <a href="/Chat/Private/@user.Id">Chat</a>
    </div>
}